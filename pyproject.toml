[tool.ruff]
line-length = 140
target-version = "py312"
exclude = [".pythonlibs", ".cache", "assets"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
